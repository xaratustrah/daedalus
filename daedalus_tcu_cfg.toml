[grafana]
address = "tcp://localhost"
port = port_number
org = "myorg"
bucket = "myexperiment"
token = "mytoken"

[tcu]
address = "tcp://localhost"
port = port_number

[lakeshore]
sensor1 = "sensor1_label"
sensor2 = "sensor2_label"
address = "tcp://localhost"
port = port_number
